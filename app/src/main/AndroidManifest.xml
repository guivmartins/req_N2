<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avaliacao.n2_http">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.N2_http">
        <activity android:name=".Follower"></activity>
        <activity android:name=".User2" />
        <activity android:name=".FollowerGit" />
        <activity android:name=".ConexaoAPI" />
        <activity android:name=".User" />
        <activity android:name=".Conversor" />
        <activity android:name=".UserGit">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>